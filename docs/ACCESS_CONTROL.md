# Access Control

## Branch Protection
- Require PR for changes under `docs/**` and `docs/STATE/**`.
- Require at least one review (owner can self-review).

## Labels
- `epoch/*`, `ci-bypass/*`, `docs/*`, `risk/*`.

## Bypass
- `ci-bypass/*` merges allowed only with owner approval; log rationale in PR description.
