# UI Toolkit

- **Primitives:** see `docs/UI/primitives.md` and imports from `@/components/ui`.
- **Forms:** see `docs/UI/forms.md` and imports from `@/components/form`.
- **Live demo:** `/ui-demo/forms` exercises validation, async submit, and toasts.

Quick import:
    import { Button, Input, Card } from "@/components/ui"
    import { Form, FormItem, FormLabel, FormControl, FormMessage } from "@/components/form"
