# History (Immutable Snapshots)

This folder stores **immutable** snapshots for completed epochs.  
Each file summarizes what changed, why, and links to PRs and prompt logs.

- Naming: `YYYY-MM-DD-epoch-####-<slug>.md`
- Never edit history snapshots after creation; create a new snapshot if corrections are needed.

---
