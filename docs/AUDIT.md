# Audit & Provenance

## Traceability Map
- STATE ↔ ROADMAP ↔ LOGS(PROMPTS/PRS) ↔ HISTORY
- `docs/STATE/INDEX.json` provides cross-refs.

## Hash Policy
- Reference commit SHAs in prompt logs and PR logs.

## Snapshots
- HISTORY files are immutable; any correction creates a new snapshot referencing the old file.
