# === Required (Auth) ===
NEXTAUTH_SECRET=
NEXTAUTH_URL=   # e.g. https://yourapp.vercel.app
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# === Required (Database) ===
MONGODB_URI=
MONGODB_DB=nextjspwa_template

# === Required (Debug Panel) ===
DEBUG_PANEL_ENABLED=false

# === Required (AI) ===
OPENAI_API_KEY=

# === Optional (AI) ===
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_BASE_URL=

# Enable guarded LangGraph endpoint (/api/ai/graph)
# AI_GRAPH_ENABLE=true

# Tracing (LangSmith) — optional
# LANGCHAIN_TRACING_V2=true
# LANGSMITH_API_KEY=
# LANGCHAIN_PROJECT=nextjspwa-template

# Debug Panel is part of the template; keep it disabled by default.
# To show it in dev only uncomment and set to true in .env.local:
# NEXT_PUBLIC_DEBUG_PANEL=true
